<h1>Teilnehmende</h1>

<div class="d-flex justify-content-end mb-2 mt-4">
  <%= link_to 'Neue Teilnehmer*in anlegen', new_participant_path, class: 'btn btn-primary btn-sm' %>
</div>

<table id="participants-table" class="table table-sm">
  <thead>
    <tr>
      <th>Nr.</th>
      <th>Kochgruppe</th>
      <th>Alter</th>
      <th>Kommentar</th>
      <th>zu berücksichtigende Ernährungsweisen</th>
      <th>Mahlzeiten</th>
    </tr>
  </thead>

  <tbody>
    <% @participants.each do |participant| %>
      <tr>
        <td data-sort="<%= participant.id %>"><%= link_to participant, participant %></td>
        <td><%= participant.group %></td>
        <td><%= participant.age %></td>
        <td><%= participant.comment %></td>
        <td><%= participant.diets.join(', ') %></td>
        <td class="text-end"><%= @meal_count[participant.id] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
